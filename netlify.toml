# Netlify build configuration (used if you deploy via Git instead of drag-and-drop)
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: route all paths to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
